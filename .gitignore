__pycache__/
*.pyc
.venv/
venv/
.env

# Raw source PDFs are copyrighted third-party documents (IFAB / FIFA) --
# not tracked in git. Place your own copies in data/raw/ before running
# ingestion. See README for sources.
data/raw/*
!data/raw/.gitkeep

data/chroma/
.pytest_cache/
*.egg-info/
# eval_report.md and eval_results.json ARE committed -- they're the real
# eval run's output, referenced directly from README.md.
.vercel
.env*
